<h1>Bookmarks</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment%>
  <%= f.submit %>
<% end %>

<%= link_to("Volver a las listas", lists_path) %>
