<div class="container">
  <h1>Index de mis listas</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
  <%= link_to "Crear nueva lista", new_list_path, class: "btn btn-primary" %>
</div>
